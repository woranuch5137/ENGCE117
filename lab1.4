#include <stdio.h>
#include <stdlib.h>

int GetSet( int mem[] ) ;

int main() {

    int *data, num ;

    num = GetSet( data ) ; //เรียกใช้ฟังก์ชั่น

    data = (int*)malloc(num * sizeof(int)) ; //จัดสรรหน่วยความจำให้กับอาเรย์
    if ( data == NULL) {
        return 1;  // หากไม่สามารถจัดสรรหน่วยความจำได้
    }// end if

    printf("Enter the elements: "); // รับจำนวนสมาชิกในเซต
    for( int i = 0 ; i < num ; i++ ) {
        scanf("%d", &data[i] ) ;

    }
    printf("Number of elements: %d\n", num); //output
    for (int i = 0; i < num; i++) {
        printf("%d ", data[i]);
    }
    printf("\n") ;

    free(data) ;

    return 0 ;
}//end main
    

int GetSet( int mem[] ) {
    int num ;
    
    printf("Enter the number of elements:"); // รับค่าจากผู้ใช้
    scanf("%d", &num ) ;

    return num ;

}//end function
