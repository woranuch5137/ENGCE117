#include <stdio.h>
#include <stdlib.h>

void GetSet( int data[], int *numNum ) ;

int main() {
    int *data, num ;
    printf( "Enter the Number of data :" ) ; //รับค่าจากผู้ใช้
    scanf( "%d", &num ) ;
    
    data = (int *)malloc(num * sizeof(int)) ; //จัดสรรหน่วยความจำให้กับอาเรย์
    if( data == NULL ) ;

    GetSet( data, &num ) ; //เรียกใช้ฟังก์ชั่น GetSet

    free(data) ; //ปล่อยหน่วยความจำ

    return 0 ;
}//end function

void GetSet( int data[], int *numNum ) {
    printf( "Enter the  data value : \n", numNum ) ; //รับข้อมูลจากผู้ใช้

    for( int i = 0 ; i < *numNum ; i++ ) {
        scanf( "%d", &data[i] ) ;
    }// end for
    for( int i = 0 ; i < *numNum ; i++ ) { //แสดงค่าที่รับเข้ามา(output)
        printf( "%d ", data[i] ) ;
    }// end for
    printf("\n") ;
} //end void
