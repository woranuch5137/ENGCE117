#include <stdio.h>
#include <string.h>

void explode( char str1[], char splitter, char str2[][100], int *count ) ;



void explode( char str1[], char splitter, char str2[][100], int *count ) {
    int i = 0, j = 0, k = 0 ;
    *count = 0 ;

    while (str1[i] != '\0')
    {
        if(str1[i] == splitter ) {
            str2[*count][j] = '\0' ;
            (*count)++ ;
            j = 0 ;
        } else {
            str2[*count][j] = str1[i] ;
            j++ ;
        }
        i++ ;
    }
    str2[*count][j] = '\0' ;
    (*count)++ ;
    
}

int main() {
    char input[] = "Mijo/Moji" ;
    char output[20][100] ;
    int num ;
    
    explode(input, '/', output, &num) ;

    for(int i = 0 ; i < num ; i++ ) {
        printf("output[%d]: %s\n", i, output[i]) ;
    }
    printf("count = %d", num ) ;
    
    return 0 ;
}//end function
